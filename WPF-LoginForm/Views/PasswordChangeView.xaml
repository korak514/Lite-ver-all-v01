<Window x:Class="WPF_LoginForm.Views.PasswordChangeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:customcontrols="clr-namespace:WPF_LoginForm.CustomControls"
        xmlns:p="clr-namespace:WPF_LoginForm.Properties"
        Title="{x:Static p:Resources.ForgotPassword}"
        Height="380" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="False"
        ShowInTaskbar="True"
        Icon="/WPF-LoginForm;component/icon.ico"
        FontFamily="Montserrat">
    <Border CornerRadius="12" Background="#1E1E1E" BorderBrush="#444444" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Color="Black" Opacity="0.6" />
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Bar with Drag -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15" MouseDown="Window_MouseDown">
                <!-- Connection indicator (green arrow, visible only when online) -->
                <Border CornerRadius="3" Padding="6,2" Background="#1B5E20"
                        Visibility="{Binding IsOnline, Converter={StaticResource BooleanToVisibilityConverter}}"
                        VerticalAlignment="Center" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x2713;" Foreground="#4CAF50" FontWeight="Bold" FontSize="14" Margin="0,0,4,0" />
                        <TextBlock Text="{x:Static p:Resources.Str_Connected}" Foreground="#81C784" FontSize="11" />
                    </StackPanel>
                </Border>
                <TextBlock Text="{x:Static p:Resources.ForgotPassword}" Foreground="White" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Error Message -->
            <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="#E57373" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,8" />

            <!-- Old Password -->
            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <TextBlock Text="{x:Static p:Resources.Str_OldPassword}" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,4" />
                <customcontrols:BindablePasswordBox Password="{Binding OldPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="32" />
            </StackPanel>

            <!-- New Password -->
            <StackPanel Grid.Row="3" Margin="0,0,0,10">
                <TextBlock Text="{x:Static p:Resources.Str_NewPassword}" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,4" />
                <customcontrols:BindablePasswordBox Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="32" />
            </StackPanel>

            <!-- Confirm New Password -->
            <StackPanel Grid.Row="4" Margin="0,0,0,5">
                <TextBlock Text="{x:Static p:Resources.Str_ConfirmNewPassword}" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,4" />
                <customcontrols:BindablePasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="32" />
            </StackPanel>

            <!-- Password Rules Hint -->
            <TextBlock Grid.Row="5" Text="{x:Static p:Resources.Str_PasswordRules}" Foreground="#888888" FontSize="10" FontStyle="Italic" TextWrapping="Wrap" Margin="0,0,0,12" />

            <!-- Buttons -->
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button Content="{x:Static p:Resources.Str_Change}"
                        Command="{Binding ChangePasswordCommand}"
                        Width="120" Height="36"
                        Background="#2E7D32" Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold" FontSize="13"
                        Cursor="Hand"
                        Margin="0,0,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#1B5E20" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bg" Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Width="100" Height="36"
                        Background="#555555" Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold" FontSize="13"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#444444" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
