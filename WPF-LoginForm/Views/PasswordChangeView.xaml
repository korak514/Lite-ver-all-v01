<Window x:Class="WPF_LoginForm.Views.PasswordChangeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:customcontrols="clr-namespace:WPF_LoginForm.CustomControls"
        Title="Şifre Değiştir"
        Width="420" MinHeight="340"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button" x:Key="DialogButtonStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="14,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="FieldLabelStyle">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Padding="28,22,28,22">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Şifre Değiştir" Foreground="White" FontSize="20" FontWeight="SemiBold" />
            </StackPanel>

            <!-- Error Message -->
            <Border Grid.Row="1" Background="#3D1A1A" CornerRadius="6" Padding="12,9" Margin="0,0,0,14"
                    BorderBrush="#E57373" BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#E57373" FontSize="12" TextWrapping="Wrap" />
            </Border>

            <!-- Old Password -->
            <StackPanel Grid.Row="2" Margin="0,0,0,14">
                <TextBlock Text="Eski Şifre" Style="{StaticResource FieldLabelStyle}" />
                <customcontrols:BindablePasswordBox Password="{Binding OldPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" />
            </StackPanel>

            <!-- New Password -->
            <StackPanel Grid.Row="3" Margin="0,0,0,14">
                <TextBlock Text="Yeni Şifre" Style="{StaticResource FieldLabelStyle}" />
                <customcontrols:BindablePasswordBox Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" />
            </StackPanel>

            <!-- Confirm New Password -->
            <StackPanel Grid.Row="4" Margin="0,0,0,6">
                <TextBlock Text="Yeni Şifre (Tekrar)" Style="{StaticResource FieldLabelStyle}" />
                <customcontrols:BindablePasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" />
            </StackPanel>

            <!-- Password Rules Hint -->
            <TextBlock Grid.Row="5" Text="• En az 6 karakter olmalı&#x0a;• Basit ve tahmin edilebilir şifreler kabul edilmez" 
                       Foreground="#666666" FontSize="11" 
                       TextWrapping="Wrap" Margin="0,10,0,18" LineHeight="16" />

            <!-- Buttons -->
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="İptal"
                        Command="{Binding CancelCommand}"
                        Width="110"
                        Background="#555555" Foreground="White"
                        Style="{StaticResource DialogButtonStyle}"
                        Margin="0,0,10,0" />
                <Button Content="Değiştir"
                        Command="{Binding ChangePasswordCommand}"
                        Width="130"
                        Background="#2E7D32" Foreground="White"
                        Style="{StaticResource DialogButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
