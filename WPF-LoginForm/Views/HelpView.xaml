<UserControl x:Class="WPF_LoginForm.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:WPF_LoginForm.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModels:HelpViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Help &amp; Support" 
                   Foreground="{StaticResource titleColor2}" 
                   FontSize="24" 
                   FontFamily="Montserrat" 
                   Margin="0,0,0,20"/>

        <!-- Content -->
        <Border Grid.Row="1" 
                CornerRadius="10" 
                Padding="20"
                BorderBrush="{StaticResource panelActiveColor}"
                BorderThickness="1">

            <!-- FIX: Explicitly create a Brush from the Color resource -->
            <Border.Background>
                <SolidColorBrush Color="{StaticResource primaryBackColor2}"/>
            </Border.Background>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding HelpContent}" 
                           Foreground="{StaticResource titleColor3}" 
                           FontSize="14" 
                           FontFamily="Montserrat" 
                           TextWrapping="Wrap"
                           LineHeight="24"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>