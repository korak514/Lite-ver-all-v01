<Window x:Class="WPF_LoginForm.Views.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:WPF_LoginForm.Properties"
        Title="{x:Static p:Resources.Str_Find}" 
        SizeToContent="WidthAndHeight" 
        MinWidth="450" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" 
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> </Grid.RowDefinitions>

        <TextBlock Text="{x:Static p:Resources.Str_FindWhat}" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,15" 
                   Foreground="#333"/>
        <TextBox x:Name="txtFind" 
                 Grid.Column="1" 
                 Height="28" 
                 Margin="0,0,0,15" 
                 VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="1" 
                   Text="{x:Static p:Resources.Str_ReplaceWith}" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,15" 
                   Foreground="#333"/>
        <TextBox x:Name="txtReplace" 
                 Grid.Row="1" 
                 Grid.Column="1" 
                 Height="28" 
                 Margin="0,0,0,15" 
                 VerticalContentAlignment="Center" />

        <CheckBox x:Name="chkMatchCase" 
                  Grid.Row="2" 
                  Grid.Column="1" 
                  Content="{x:Static p:Resources.Str_MatchCase}" 
                  Margin="0,0,0,20" />

        <StackPanel Grid.Row="3" 
                    Grid.Column="0" 
                    Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            
            <Button Content="{x:Static p:Resources.Str_Find}" 
                    Width="90" 
                    Height="32" 
                    Margin="0,0,10,0" 
                    Click="Find_Click" 
                    IsDefault="True" />
            
            <Button Content="{x:Static p:Resources.Str_Replace}" 
                    Width="90" 
                    Height="32" 
                    Click="Replace_Click" />
        </StackPanel>
    </Grid>
</Window>