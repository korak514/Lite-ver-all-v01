<Window x:Class="WPF_LoginForm.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_LoginForm.Views"
        mc:Ignorable="d"
        Title="Configuration" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Data Source:" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding Tables}" SelectedItem="{Binding SelectedTable}"/>
            <TextBlock Text="X-Axis Column:" FontWeight="Bold" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding Columns}" SelectedItem="{Binding SelectedXAxisColumn}"/>

            <TextBlock Text="Y-Axis Columns:" FontWeight="Bold" Margin="0,10,0,0"/>
            <ListBox ItemsSource="{Binding SelectableColumns}" SelectionMode="Multiple">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock Text="Chart Type:" FontWeight="Bold" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding ChartTypes}" SelectedItem="{Binding SelectedChartType}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="OK" Width="80" Margin="5" Command="{Binding OkCommand}"/>
                <Button Content="Cancel" Width="80" Margin="5" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
