<Window x:Class="WPF_LoginForm.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:WPF_LoginForm.ViewModels"
        xmlns:customcontrols="clr-namespace:WPF_LoginForm.CustomControls"
        xmlns:p="clr-namespace:WPF_LoginForm.Properties"
        xmlns:converters="clr-namespace:WPF_LoginForm.Converters"
        mc:Ignorable="d"
        Title="LoginView" Height="550" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <viewModel:LoginViewModel />
    </Window.DataContext>

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
        <converters:BooleanInverterConverter x:Key="BooleanInverter" />
    </Window.Resources>

    <Window.Visibility>
        <Binding Path="IsViewVisible" Mode="TwoWay" Converter="{StaticResource BooleanToVisibility}" />
    </Window.Visibility>

    <Border CornerRadius="12">
        <Border.Background>
            <ImageBrush ImageSource="/Images/biosun/bg.jpg" Stretch="None" />
        </Border.Background>

        <Border CornerRadius="10" BorderThickness="2" Opacity="0.89">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource winBorderColor1}" Offset="0" />
                    <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.60" />
                    <GradientStop Color="{StaticResource winBorderColor3}" Offset="1" />
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0.15" />
                    <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0.55" />
                    <GradientStop Color="{StaticResource primaryBackColor1}" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <!-- Window Controls -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="5" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Static p:Resources.LoginTitle}"
                               Foreground="DarkGray" FontSize="10" FontFamily="Montserrat"
                               Grid.Column="0" VerticalAlignment="Center" Margin="10,0,0,0" />

                    <Button x:Name="btnMinimize" Content="-" Style="{StaticResource WindowControlButtonStyle}" Grid.Column="1" Click="btnMinimize_Click" />
                    <Button x:Name="btnClose" Content="X" Style="{StaticResource WindowCloseButtonStyle}" Grid.Column="2" Click="btnClose_Click" />
                </Grid>

                <!-- Main Content -->
                <StackPanel Width="220" Grid.Row="1" Orientation="Vertical" Margin="0,35,0,0">
                    <Image Source="/Images/biosun/logo2.png" Width="150" Height="122" />

                    <TextBlock Text="Veri Giriş Ve Raporlama" Foreground="White" FontSize="18" FontWeight="Medium" FontFamily="Montserrat" HorizontalAlignment="Center" />
                    <TextBlock Foreground="LightGray" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" TextWrapping="Wrap" TextAlignment="Center" Margin="0,5,0,0" />

                    <!-- Inputs Panel (Disabled when Busy) -->
                    <StackPanel IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInverter}}">
                        <!-- Username -->
                        <TextBlock Text="{x:Static p:Resources.Username}" Foreground="DarkGray" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" Margin="0,35,0,0" />
                        <TextBox x:Name="txtUser" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="13" FontWeight="Medium" FontFamily="Montserrat" Foreground="White"
                                 CaretBrush="LightGray" BorderBrush="DarkGray" BorderThickness="0,0,0,2"
                                 Height="28" VerticalContentAlignment="Center" Margin="0,5,0,0" Padding="20,0,0,0">
                            <TextBox.Background>
                                <ImageBrush ImageSource="/Images/user-icon.png" Stretch="None" AlignmentX="Left" />
                            </TextBox.Background>
                        </TextBox>

                        <!-- Password -->
                        <TextBlock Text="{x:Static p:Resources.Password}" Foreground="DarkGray" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" Margin="0,15,0,0" />
                        <customcontrols:BindablePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,5,0,0" />

                        <!-- Report Mode Switch -->
                        <CheckBox Content="{x:Static p:Resources.ReportMode}"
                                  IsChecked="{Binding IsReportModeOnly, Mode=TwoWay}"
                                  Foreground="LightGreen" FontFamily="Montserrat" FontSize="12"
                                  Margin="0,15,0,0" HorizontalAlignment="Center" VerticalContentAlignment="Center" />
                    </StackPanel>

                    <!-- Error Message -->
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#D7596D" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" Margin="0,10,0,0" TextWrapping="Wrap" />

                    <!-- Login Button Area -->
                    <Grid Margin="0,30,0,0">
                        <Button x:Name="btnLogin"
                                Command="{Binding LoginCommand}"
                                Content="{x:Static p:Resources.LoginButton}"
                                Foreground="White"
                                FontSize="12"
                                FontFamily="Montserrat"
                                Cursor="Hand"
                                FocusVisualStyle="{x:Null}">

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <!-- Explicit Border to ensure no white lines -->
                                    <Border x:Name="btnBorder"
                                            Width="150" Height="40"
                                            CornerRadius="20"
                                            Background="Green"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="btnBorder" Property="Background" Value="#0C5F11" />
                                        </Trigger>
                                        <!-- Dim when disabled (IsBusy) -->
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="btnBorder" Property="Opacity" Value="0.5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <TextBlock Text="{x:Static p:Resources.ForgotPassword}" Foreground="DarkGray" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" />
                        <TextBlock Text="Reset" Foreground="White" FontSize="12" FontWeight="Medium" FontFamily="Montserrat" Cursor="Hand" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>

                <!-- Offline Settings Button -->
                <Button Grid.Row="1"
                        Content="--&gt; Settings"
                        Command="{Binding OpenSettingsCommand}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Margin="15,0,0,15"
                        Background="Transparent"
                        Foreground="Gray"
                        FontSize="12"
                        Cursor="Hand"
                        BorderThickness="0"
                        FocusVisualStyle="{x:Null}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter VerticalAlignment="Center" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Border>
</Window>