<!-- Views/ChartDetailView.xaml -->
<UserControl x:Class="WPF_LoginForm.Views.ChartDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:WPF_LoginForm.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:viewModels="clr-namespace:WPF_LoginForm.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=viewModels:ChartDetailViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>

        <!-- THE CUSTOM TOOLTIP INSTANCE -->
        <local:CustomChartTooltip x:Key="CustomTooltip" x:Shared="False" />

        <DataTemplate x:Key="SmartLabelTemplate">
            <Border Background="#E6000030" CornerRadius="4" Padding="6,4" BorderBrush="#80FFFFFF" BorderThickness="1">
                <TextBlock Text="{Binding ChartPoint.Instance.Label}"
                           Foreground="White"
                           FontSize="10"
                           FontWeight="Bold"
                           TextAlignment="Center" />
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="#0D1263">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0A0F8F" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding GoBackCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="5,0">
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="ArrowLeft" Height="16" Foreground="#FFC047" Margin="0,0,10,0" />
                        <TextBlock Text="Return to Dashboard" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#181838" CornerRadius="10" Margin="15" Padding="20">
            <!-- UPDATED: Added DataTooltip binding -->
            <lvc:CartesianChart Series="{Binding ChartSeries}"
                                AxisX="{Binding ChartX}"
                                AxisY="{Binding ChartY}"
                                LegendLocation="Bottom"
                                AnimationsSpeed="0:0:0.4"
                                DataTooltip="{StaticResource CustomTooltip}">
                <lvc:CartesianChart.ChartLegend>
                    <lvc:DefaultLegend Foreground="WhiteSmoke" FontSize="14" />
                </lvc:CartesianChart.ChartLegend>
            </lvc:CartesianChart>
        </Border>
    </Grid>
</UserControl>