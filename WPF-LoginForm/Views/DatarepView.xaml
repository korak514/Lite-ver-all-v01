<UserControl x:Class="WPF_LoginForm.Views.DatarepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_LoginForm.Views"
             xmlns:converters="clr-namespace:WPF_LoginForm.Converters"
             xmlns:systemData="clr-namespace:System.Data;assembly=System.Data"
             xmlns:viewModels="clr-namespace:WPF_LoginForm.ViewModels"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:p="clr-namespace:WPF_LoginForm.Properties"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:DatarepViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanInverterConverter x:Key="BooleanInverter" />
        <converters:IsRowEditableConverter x:Key="IsRowEditableConverter" />
        <converters:IsItemInCollectionConverter x:Key="IsItemInCollectionConverter" />
        <converters:IsNullOrEmptyConverter x:Key="IsNullOrEmptyConverter" />

        <Style x:Key="ErrorMessageTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Red" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="AcceptsReturn" Value="False" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="0 0 0 2" Padding="10">
            <ToolBarTray IsLocked="True">

                <!-- Toolbar 1: Main Actions -->
                <ToolBar Band="1" BandIndex="1" ToolBar.OverflowMode="Never">
                    <Label Content="{x:Static p:Resources.Table}" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding TableNames}" SelectedItem="{Binding SelectedTable, Mode=TwoWay}" DisplayMemberPath="." MinWidth="150" VerticalAlignment="Center" Margin="0,0,5,0" />

                    <Button Content="{x:Static p:Resources.Delete}" ToolTip="Permanently delete table" Margin="0,0,10,0" Command="{Binding DeleteTableCommand}" Foreground="Red"
                            Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Button Content="{x:Static p:Resources.Add}" ToolTip="{x:Static p:Resources.Add}" Margin="4,0" Command="{Binding AddNewRowCommand}" />
                    <Button Content="{x:Static p:Resources.Delete}" ToolTip="{x:Static p:Resources.Delete}" Margin="4,0" Command="{Binding DeleteSelectedRowCommand}" CommandParameter="{Binding ElementName=DataDisplayGrid, Path=SelectedItems}" />
                    <Separator Margin="4,0" />

                    <Button Content="{x:Static p:Resources.Save}" ToolTip="{x:Static p:Resources.Save}" Margin="4,0" Command="{Binding SaveChangesCommand}" IsEnabled="{Binding IsDirty}" />
                    <Button Content="{x:Static p:Resources.Undo}" ToolTip="{x:Static p:Resources.Undo}" Margin="4,0" Command="{Binding UndoChangesCommand}" IsEnabled="{Binding UndoChangesCommand.CanExecute, Mode=OneWay}" />

                    <Separator Margin="4,0" />

                    <CheckBox Content="{x:Static p:Resources.LoadFullHistory}"
                              IsChecked="{Binding LoadAllData, Mode=TwoWay}"
                              ToolTip="{x:Static p:Resources.Tip_LoadFullHistory}"
                              VerticalAlignment="Center" Margin="4,0" />

                    <Button Content="{x:Static p:Resources.Reload}" ToolTip="{x:Static p:Resources.Reload}" Margin="4,0" Command="{Binding ReloadDataCommand}" />

                    <Separator Margin="4,0" />
                    <Button Content="{x:Static p:Resources.Edit}" ToolTip="{x:Static p:Resources.Edit}" Margin="4,0" Command="{Binding EditSelectedRowsCommand}" CommandParameter="{Binding ElementName=DataDisplayGrid, Path=SelectedItems}" />
                    <Button Content="{x:Static p:Resources.SelectAll}" ToolTip="{x:Static p:Resources.SelectAll}" Margin="4,0,0,0" Click="SelectAllButton_Click" />

                    <!-- Find & Replace Button -->
                    <Button Command="{Binding ShowFindReplaceCommand}"
                        ToolTip="{x:Static p:Resources.Str_Find}"
                        Background="Transparent"
                        BorderThickness="0"
                        Margin="5,0,5,0">
                        <StackPanel Orientation="Horizontal">
                            <!-- Using FontAwesome.WPF control -->
                            <fa:ImageAwesome Icon="Search"
                         Width="15"
                         Height="15"
                         Foreground="{DynamicResource color1}"
                         VerticalAlignment="Center" />

                            <TextBlock Text="{x:Static p:Resources.Str_Find}"
                         Margin="5,0,0,0"
                         VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush2}" />
                        </StackPanel>
                    </Button>
                </ToolBar>

                <!-- Toolbar 2: Filtering and Display -->
                <ToolBar Band="2" BandIndex="1" ToolBar.OverflowMode="Never">
                    <Label Content="{x:Static p:Resources.QuickSearch}" VerticalAlignment="Center" />
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" MinWidth="170" VerticalAlignment="Center" />
                    <!-- NEW CHECKBOX -->
                    <CheckBox Content="Global"
                              IsChecked="{Binding IsGlobalSearchActive}"
                              ToolTip="Search in ALL columns"
                              VerticalAlignment="Center"
                              Margin="5,0,5,0" />
                    <ToggleButton IsChecked="{Binding IsColumnSelectorVisible, Mode=TwoWay}" ToolTip="Options" Margin="4,0,0,0">
                        <fa:ImageAwesome Icon="Gear" Height="14" Foreground="DimGray" />
                    </ToggleButton>

                    <ComboBox ItemsSource="{Binding SearchableColumns}" SelectedItem="{Binding SelectedSearchColumn, Mode=TwoWay}" MinWidth="140" VerticalAlignment="Center" Margin="2,0,5,0"
                              Visibility="{Binding IsColumnSelectorVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <!-- NEW: Options Panel with Rename Button -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsColumnSelectorVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button Content="{x:Static p:Resources.AddId}" Command="{Binding AddIdColumnCommand}" Margin="5,0" Background="#FFF0F0F0" BorderBrush="Gray" />

                        <!-- RENAME BUTTON -->
                        <Button Command="{Binding RenameColumnCommand}"
            ToolTip="{x:Static p:Resources.Tip_RenameColumn}"
            Margin="5,0"
            Background="#FFF0F0F0"
            BorderBrush="Gray">
                            <StackPanel Orientation="Horizontal">
                                <fa:ImageAwesome Icon="Pencil" Height="12" Foreground="DimGray" Margin="0,0,5,0" />
                                <TextBlock Text="{x:Static p:Resources.RenameColumn}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <CheckBox Content="{x:Static p:Resources.HideId}" IsChecked="{Binding IsIdHidden, Mode=TwoWay}" VerticalAlignment="Center" Margin="5,0" />
                        <CheckBox Content="{x:Static p:Resources.EditId}" IsChecked="{Binding IsIdEditable, Mode=TwoWay}" VerticalAlignment="Center" Margin="5,0" />

                        <CheckBox Content="Show Adv. Import" IsChecked="{Binding IsAdvancedImportVisible, Mode=TwoWay}" VerticalAlignment="Center" Margin="10,0,5,0"
                                  ToolTip="Show Import Table/Map buttons" />
                    </StackPanel>

                    <Button Content="{x:Static p:Resources.Clear}" ToolTip="{x:Static p:Resources.Clear}" Margin="4,0" Command="{Binding ClearSearchCommand}" />
                    <Separator Margin="4,0" />

                    <ToggleButton IsChecked="{Binding IsDateFilterPanelVisible, Mode=TwoWay}"
                                  ToolTip="{x:Static p:Resources.FilterbyDate}" Margin="4,0,0,0"
                                  Visibility="{Binding IsDateFilterVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <fa:ImageAwesome Icon="Calendar" Height="14" Foreground="DimGray" />
                    </ToggleButton>

                    <Separator Margin="4,0" />
                    <Button Content="{x:Static p:Resources.Import}" ToolTip="{x:Static p:Resources.Import}" Margin="4,0" Command="{Binding ImportDataCommand}" />

                    <Button Content="Import Table" ToolTip="Advanced Import" Margin="4,0"
                            Command="{Binding ShowAdvancedImportCommand}"
                            Visibility="{Binding IsAdvancedImportVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Button ToolTip="Import Data Entry Logic" Margin="4,0"
                            Command="{Binding ShowHierarchyImportCommand}"
                            Visibility="{Binding IsAdvancedImportVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Sitemap" Height="14" Foreground="DimGray" Margin="0,0,5,0" />
                            <TextBlock Text="Import Map" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Content="{x:Static p:Resources.CreateNewTable}"
                            ToolTip="{x:Static p:Resources.Tip_CreateNewTable}"
                            Margin="4,0"
                            Command="{Binding ShowCreateTableCommand}" />

                    <Button Content="{x:Static p:Resources.Export}" ToolTip="{x:Static p:Resources.Export}" Margin="4,0" Command="{Binding ExportDataCommand}" />
                    <Separator Margin="4,0" />
                    <Button Content="A-" Margin="4,0" Command="{Binding DecreaseFontSizeCommand}" />
                    <Button Content="A+" Margin="4,0" Command="{Binding IncreaseFontSizeCommand}" />
                </ToolBar>

                <!-- Toolbar 3: Date Filter Panel -->
                <ToolBar Band="3" BandIndex="1" ToolBar.OverflowMode="Never"
                         Visibility="{Binding IsDateFilterPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Label Content="{x:Static p:Resources.FilterbyDate}" VerticalAlignment="Center" />
                    <DatePicker SelectedDate="{Binding FilterStartDate, Mode=TwoWay}" VerticalAlignment="Center" Margin="4,0" />
                    <Label Content="to" VerticalAlignment="Center" Margin="5,0" />
                    <DatePicker SelectedDate="{Binding FilterEndDate, Mode=TwoWay}" VerticalAlignment="Center" Margin="4,0,10,0" />

                    <xctk:RangeSlider MinWidth="250" VerticalAlignment="Center"
                                      Minimum="0" Maximum="{Binding SliderMaximum}"
                                      LowerValue="{Binding StartMonthSliderValue, Mode=TwoWay}"
                                      HigherValue="{Binding EndMonthSliderValue, Mode=TwoWay}"
                                      IsSnapToTickEnabled="True"
                                      TickFrequency="1" />

                    <Button Content="{x:Static p:Resources.Clear}" Command="{Binding ClearDateFilterCommand}" Margin="10,0,0,0" />
                </ToolBar>
            </ToolBarTray>
        </Border>

        <!-- Progress & Errors -->
        <Grid Grid.Row="1" MinHeight="25" Margin="10,5,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ProgressBar Grid.Column="0" IsIndeterminate="True" Width="150" Height="15" VerticalAlignment="Center"
                         Visibility="{Binding IsProgressBarVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <TextBox Grid.Column="1" Text="{Binding ErrorMessage, Mode=OneWay}" Style="{StaticResource ErrorMessageTextBoxStyle}" Margin="10,0,0,0"
                     Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <TextBlock Grid.Column="2" Text="{Binding FilterStatus}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FF5555" />
        </Grid>

        <!-- Data Grid -->
        <Grid Grid.Row="2">
            <DataGrid x:Name="DataDisplayGrid"
                      ItemsSource="{Binding DataTableView}"
                      AutoGenerateColumns="True"

                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Extended"
                      AlternationCount="2"
                      GridLinesVisibility="All"

                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      FontSize="{Binding DataGridFontSize, FallbackValue=12}"

                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      VirtualizingPanel.CacheLength="1,1"
                      VirtualizingPanel.CacheLengthUnit="Page"
                      ScrollViewer.CanContentScroll="True"

                      AutoGeneratingColumn="DataDisplayGrid_AutoGeneratingColumn"
                      BeginningEdit="DataDisplayGrid_BeginningEdit"
                      Sorting="DataDisplayGrid_Sorting">

                <DataGrid.IsReadOnly>
                    <Binding Path="EditableRows" Converter="{StaticResource IsRowEditableConverter}" />
                </DataGrid.IsReadOnly>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="Gainsboro" />
                            </Trigger>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource IsItemInCollectionConverter}">
                                        <Binding />
                                        <Binding Path="DataContext.EditableRows" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter Property="Background" Value="{StaticResource statusEditableColor}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Row.RowState}" Value="{x:Static systemData:DataRowState.Modified}">
                                <Setter Property="Background" Value="{StaticResource statusModifiedColor}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Row.RowState}" Value="{x:Static systemData:DataRowState.Added}">
                                <Setter Property="Background" Value="{StaticResource statusAddedColor}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>